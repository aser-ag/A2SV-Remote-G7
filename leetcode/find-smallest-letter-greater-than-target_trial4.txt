class Solution:
    def nextGreatestLetter(self, letters: List[str], target: str) -> str:
        smallest = "z"
        found = False
        for letter in letters:
            if letter > target and letter <= smallest:
                smallest = letter
                found = True
        
        if found:
            return smallest
        else:
            return letters[0]