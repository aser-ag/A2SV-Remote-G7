class Solution:
    def firstUniqChar(self, s: str) -> int:
        counter = [0]*26

        for char in s:
            counter[ord(char) - 97] += 1

        for i in range(len(s)):
            if counter[ord(s[i]) - 97] == 1:
                return i            

        return -1